server:
  port: 8082

spring:
  application:
    name: variance-service

# 下游服务地址
variance:
  ai-service:
    base-url: http://localhost:8081
  data-analysis:
    base-url: http://localhost:8888

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
